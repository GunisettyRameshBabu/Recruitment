import { ProgressBar } from '../progressbar';
/**
 * Animation for progress bar
 */
export declare class ProgressAnimation {
    /** Linear Animation */
    doLinearAnimation(element: Element, progress: ProgressBar, delay: number, start?: number): void;
    /** Linear Indeterminate */
    doLinearIndeterminate(element: Element, progress: ProgressBar): void;
    /** Circular animation */
    doCircularAnimation(x: number, y: number, radius: number, start: number, progressEnd: number, value: number, element: Element, progress: ProgressBar, thickness: number, delay: number, startValue?: number): void;
    /** Circular indeterminate */
    doCircularIndeterminate(circularProgress: HTMLElement, progress: ProgressBar, start: number, end: number, x: number, y: number, radius: number, thickness: number): void;
    /** To do the label animation for progress bar */
    doLabelAnimation(labelPath: Element, start: number, end: number, progress: ProgressBar, delay: number): void;
    /** To do the annotation animation for circular progress bar */
    doAnnotationAnimation(circularPath: Element, progress: ProgressBar): void;
}
